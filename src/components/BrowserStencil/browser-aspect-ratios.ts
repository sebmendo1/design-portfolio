/** Default Salesforce Help demo recording (1280×854). */
export const DEFAULT_BROWSER_SCREEN_AR = 1280 / 854;

/** WRITER ReWrite demo recording (2922×1854). */
export const WRITER_REWRITE_SCREEN_AR = 2922 / 1854;

/** Chorus AI product demo (1920×1080). */
export const CHORUS_DEMO_SCREEN_AR = 1920 / 1080;

/** Reference frame width for proportional chrome scaling. */
export const BROWSER_REFERENCE_WIDTH = 480;

/** Below this laid-out width, browser chrome drops nav + spacer. */
export const BROWSER_COMPACT_WIDTH = 360;

/** Below this laid-out width, browser chrome keeps only traffic lights. */
export const BROWSER_TINY_WIDTH = 240;
