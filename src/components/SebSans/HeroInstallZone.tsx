'use client';

import { InstallPanels } from '@/components/SebSans/InstallPanels';

export function HeroInstallZone() {
  return (
    <div id="install">
      <InstallPanels variant="hero" />
    </div>
  );
}
